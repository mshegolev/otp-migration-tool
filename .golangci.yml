version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - errcheck
    - gofmt
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unused
  exclusions:
    paths:
      - internal/migration/pb         # generated protobuf code
      - examples
